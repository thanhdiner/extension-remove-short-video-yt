// Giữ trống (Manifest V3 service worker).
// Nếu sau này muốn thêm rule chặn network cho /shorts/, có thể dùng declarativeNetRequest.
// Ở bản hiện tại, content script đã xử lý redirect/ẩn là đủ gọn & ổn định.
